const newuser = model.inputs.profile.newuser
const existingusers = model.data.users

function createNewUser(){
    
}

function checkPassword(){
    if (user.password === user.repeatpassword){
        //så pusher vi newuser til users
    }
}